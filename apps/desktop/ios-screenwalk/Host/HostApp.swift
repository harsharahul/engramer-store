import SwiftUI

/// Placeholder host: Xcode requires a UI test target to name an app. The
/// walk itself drives the installed Engram Store app by bundle id.
@main
struct HostApp: App {
  var body: some Scene {
    WindowGroup { Text("Screen walk host") }
  }
}
